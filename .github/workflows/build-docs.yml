name: "Build docs"

on:
  push:
    branches:
      - main
      - deploy-docs-fix-2

jobs:
  build-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        ssh-key: "95:33:49:57:f9:11:81:75:a4:46:74:19:c6:89:f4:1c"
    - name: Setup python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
        cache: 'pip'
    - name: Install dependencies
      run: |
        python3.10 -m venv venv/
        venv/bin/python -m pip install -r .script/requirements.txt
        venv/bin/python -m pip install lib/metric-config-parser
    - name: Build docs
      run: |
        venv/bin/python .script/generate_docs.py \
          --output_dir=generated_docs/
        cd generated_docs/
        PATH="../venv/bin:$PATH" mkdocs gh-deploy --force \
          -m "[ci skip] Deployed {sha} with MkDocs version: {version}"
